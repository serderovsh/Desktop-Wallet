// @flow
import React, { Component } from 'react';
import styles from './AppWrapper.css';

type Props = {};

export default class AppWrapper extends Component<Props> {
  props: Props;

  render() {
    return (
        <div className={styles.appWrapper}>
        
        </div>
    );
  }
}
